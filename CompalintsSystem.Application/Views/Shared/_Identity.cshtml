@using CompalintsSystem.Core.Models;
@using Microsoft.AspNetCore.Identity
@inject UserManager<ApplicationUser> UserManager;
@inject SignInManager<ApplicationUser> signInManager;

        @if (signInManager.IsSignedIn(User))
        {
            var currentUser = await UserManager.GetUserAsync(User);

            var userName = currentUser.FullName;
            var image = currentUser.ProfilePicture;


            <ul class="dropdown-menu dropdown-menu-end">
                <li>
                    <a class="dropdown-item">
                        <div class="d-flex">
                            <div class="flex-shrink-0 me-3">
                                <div class="avatar avatar-online">
    @if (UserManager.GetUserAsync(User).Result.ProfilePicture != null)
    {
<img style="width: 40px; height: 40px; object-fit: cover; border-radius: 30px" src="~/assets/img/branding/logoName.png">    }
    else
    {
        <img style="width: 40px; height: 40px; object-fit: cover; border-radius: 30px" src="~/assets/img/branding/logoName.png">
    }
</div>
@*<div class="avatar avatar-online">
    @if (UserManager.GetUserAsync(User).Result.ProfilePicture != null)
    {
        var imagePath = "~/Images/" + UserManager.GetUserAsync(User).Result.ProfilePicture;
        <img style="width: 40px; height: 40px; object-fit: cover; border-radius: 30px" src="@imagePath">
    }
    else
    {
        <img style="width: 40px; height: 40px; object-fit: cover; border-radius: 30px" src="~/assets/img/branding/logoName.png">
    }
</div>
*@
                            </div>
                            <div class="flex-grow-1">

                                <span class="lh-1 d-block fw-semibold"> @userName</span>

                                @if (User.IsInRole("AdminGeneralFederation"))
                                {
                                    <small>
                                        رئيس جامعة
                                    </small>
                                }
                                else if (User.IsInRole("AdminColleges"))
                                {
                                    <small>
                                        عميد كلية
                                    </small>
                                }
                                else if (User.IsInRole("AdminDepartments"))
                                {
                                    <small>
                                        رئيس قسم
                                    </small>
                                }
                                else if (User.IsInRole("AdminSubDepartments"))
                                {
                                    <small>
                                        موظف شكوى 
                                    </small>
                                }
                                else if (User.IsInRole("AdminVillages"))
                                {
                                    <small>
                                       طالب
                                    </small>
                                }
                                else if (User.IsInRole("Beneficiarie"))
                                {
                                    <small>
                                        طالب
                                    </small>
                                }

                            </div>
                        </div>
                    </a>
                </li>
                <li>
                    <div class="dropdown-divider"></div>
                </li>

                <li>
                    <a class="dropdown-item" asp-controller="ManageProfile" asp-action="Profile">
                        <i class="bx bx-user me-2"></i><span class="align-middle">الملف الجامعي</span>
                    </a>
                </li>


                <li>

                    <div class="dropdown-divider"></div>
                </li>
                <li>

                    <a class="dropdown-item" asp-area="" asp-controller="Account" asp-action="LogOut">
                        <i class="bx bx-power-off me-2"></i>
                        <span class="align-middle">تسجيل الخروج</span>
                    </a>
                </li>


            </ul>
        }
